apiVersion: v1
kind: Service
metadata:
  name: bookmyshow-service
spec:
  selector:
    app: bookmyshow
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 31000

